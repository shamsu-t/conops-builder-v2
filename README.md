# ConOps Builder v2

Backend: FastAPI + Pydantic + SQLModel (SQLite)
Frontend: React + TypeScript + Vite + MUI

## Dev
- Backend: uvicorn app.main:app --reload --port 5071
- Frontend: npm run dev (default 5173)

## Notes
- File-based save/load as JSON for portability
- SQLite for local project storage
